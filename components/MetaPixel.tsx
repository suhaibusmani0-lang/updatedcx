"use client";

/**
 * Kept only for backwards compatibility with older imports.
 * The real Pixel loader is components/Analytics.tsx. Keeping this component
 * inert prevents accidental second initialization/PageView calls.
 */
export default function MetaPixel() {
  return null;
}
